services:

  db:
    image: mysql
    env_file:
      - .env
    ports:
      - 3306:3306
    networks:
      - jera

  api:
    env_file:
      - .env
    build:
      context: ./backend/
      dockerfile: Dockerfile
    ports:
      - 8000:8000
    logging:
      driver: "json-file"
      options:
        max-size: "500k"
        max-file: "20"
    restart: unless-stopped
    depends_on:
      - db
    networks:
      - jera
    command: sh -c "./wait-for-it.sh db:3306 -- uvicorn app.main:api --host 0.0.0.0 --port 8000"    

networks:
  jera:
    driver: bridge